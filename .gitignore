node_modules/
data/rooms.json
.DS_Store
*.log

# редакторское/агентское, к продукту отношения не имеет
.claude/

# Telegram-бот: локальная база столов
data/bot.db
data/bot.db-*

# токен бота и прочие секреты — никогда в git
.env
.env.*
!.env.example

# скриншоты стола (npm run preview) — собираются заново, в git не нужны
preview/
