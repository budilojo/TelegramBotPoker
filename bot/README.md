# Покер и дурак в Telegram

Игровой хаб для компании друзей: **покер** (техасский холдем) и **дурак**
(подкидной или переводной) в Telegram Mini App. В группе `/game` → в
приложении выбираете игру → в группе появляется карточка лобби → друзья
присоединяются → партия идёт в приложении → итоги приходят в группу. В одной
группе можно играть в несколько игр сразу.

- **Покер** — зелёное сукно, свои карты внизу, борд и банк по центру, игроки
  по кругу. Бот тасует, сдаёт, считает банк и сайд-поты — или, в режиме
  дилера, игра идёт настоящими картами, а бот считает фишки.
- **Дурак** — свои карты веером внизу, на столе пары «атака/защита», колода с
  козырем, соперники сверху с числом карт; видно, кто ходит, кто отбивается,
  кто может подкинуть. Серия партий со счётом «кто сколько раз дурак».

Фишки виртуальные. Никаких настоящих денег, платежей и ставок на деньги.

Как и почему устроен хаб и дурак — [docs/game-hub.md](../docs/game-hub.md);
интерфейс покерного стола — [docs/miniapp-ux.md](../docs/miniapp-ux.md).

```
Группа ──/game──▶ Бот ──«🎮 Во что играем? [Выбрать игру]»──▶ Группа
                   │
Кнопка ──t.me/<бот>/<приложение>?startapp=g_<код группы>──▶ хаб: «Покер» / «Дурак»
                   │                                          │ «Создать лобби»
Группа ◀── карточка лобби «🃏 Дурак · 1/6 · [Присоединиться]» ─┘
                   │
Кнопка ──t.me/<бот>/<приложение>?startapp=<код игры>──▶ стол (у каждого свой)
                   └── WebSocket: состояние стола ◀──▶ ходы
```

---

## Запуск одной командой

```bash
npm run go
```

Эта команда поднимает туннель, **сама вписывает его адрес в `.env`**,
проверяет, что стол виден снаружи, и запускает бота — всё в одном окне.
Пока окно открыто, игра работает; `Ctrl+C` гасит и туннель, и бота.

Бесплатный адрес cloudflared меняется при каждом запуске. Когда он сменился,
команда показывает его в рамке: этот адрес надо вписать у @BotFather
(`/myapps` → ваше приложение → **Edit Web App URL**) — единственное, что
осталось делать руками.

Если что-то не так, команда говорит что именно: не установлен `cloudflared`,
нет токена, порт занят другим ботом, Telegram не принял токен.

Ниже — то же самое по частям (`npm run bot` + отдельный туннель), если
хочется запускать их раздельно.

---

## Обновление: что сделать, если бот уже запущен

Всё, что было, работает как раньше: `/newgame` делает покер, старые кнопки
«Открыть стол» открывают свои столы, база переносится сама.

1. Остановите бота в Терминале: `Ctrl+C`.
2. Скачайте новую версию: `git pull`.
3. Запустите: `npm run go` — туннель и бот поднимутся вместе.
4. В группе напишите `/game`. Придёт карточка «🎮 Во что играем?» — нажмите
   «Выбрать игру», выберите «Дурак» → «Создать лобби».
5. В группе появится карточка «🃏 Дурак». Друзья жмут «Присоединиться» →
   «Сесть за стол». Вы жмёте «▶ Начать».

В @BotFather менять ничего не нужно: короткое имя приложения (`table`)
остаётся, меню команд с `/game` бот обновит сам при запуске.

---

## Запуск

Mini App — это веб-страница, и Telegram открывает её **только по HTTPS**.
Поэтому кроме токена нужны две вещи: публичный HTTPS-адрес этого процесса и
регистрация приложения у @BotFather. Ниже — как сделать это на своём Маке за
10 минут.

### 1. Туннель: HTTPS-адрес для ноутбука

```bash
brew install cloudflared
cloudflared tunnel --url http://localhost:8080
```

В выводе будет адрес вида `https://something-words.trycloudflare.com` —
это и есть адрес стола. Окно с туннелем не закрывайте, пока играете.

### 2. Приложение у @BotFather

`@BotFather` → `/newapp` → выбрать своего бота → название, описание,
картинка 640×360 (любая) → GIF можно пропустить (`/empty`) →
**Web App URL** — адрес из туннеля → **короткое имя**, например `table`.

BotFather ответит ссылкой `t.me/<бот>/table` — значит, всё правильно.

### 3. `.env`

```bash
BOT_TOKEN=...                                   # от @BotFather, как раньше
WEBAPP_URL=https://something-words.trycloudflare.com
MINIAPP=table                                   # короткое имя из /newapp
```

### 4. Бот

```bash
npm install
npm run go
```

В консоли: `стол: http://localhost:8080 → https://…trycloudflare.com` и
`long polling запущен`. В группе: `/game` → «🎮 Выбрать игру» (или сразу
`/newgame` → «🃏 Открыть стол» для покера).

**Бесплатный адрес cloudflared меняется при каждом перезапуске туннеля.**
Тогда: новый адрес → в `.env` (`WEBAPP_URL`) и у @BotFather (`/myapps` →
ваше приложение → изменить Web App URL), затем перезапустить бота. Постоянный
адрес дают именованный туннель cloudflared со своим доменом или хостинг.

Без `MINIAPP` всё тоже работает, но на шаг длиннее: кнопка в группе ведёт в
личку с ботом, и уже там бот даёт кнопку, открывающую стол (кнопки
мини-приложения Telegram разрешает только в личке).

| Переменная | По умолчанию | Зачем |
|---|---|---|
| `BOT_TOKEN` | — | обязательна |
| `WEBAPP_URL` | — | публичный HTTPS-адрес; без него стол не откроется |
| `MINIAPP` | — | короткое имя из `/newapp`; без него — через личку |
| `PORT` | `8080` | где слушает сервер стола (туннель смотрит сюда) |
| `DB_PATH` | `./data/bot.db` | где лежат столы |
| `DRAW_INTERVAL_MS` | `1000` | как часто можно править карточку в группе |
| `TZ` | системная | часовой пояс в напоминаниях |

`.env` в git не попадает. На хостинге задайте переменные в настройках
сервиса — они важнее файла. **Токен никому не пересылайте и не
фотографируйте**; если он утёк — `@BotFather` → `/revoke`.

Privacy mode бота оставьте включённым: читать переписку группы боту не нужно.
Права администратора тоже не нужны.

---

## Как играть

### Выбор игры — `/game`

Приложение можно открыть и без группы — из профиля бота или командой `/game`
у него в личке: оно покажет ваши группы (где вы писали боту или играли), а
если группа одна — сразу её игры. Чужих групп оно не покажет.

1. В группе `/game`. Бот присылает карточку «🎮 Во что играем?».
2. «🎮 Выбрать игру» открывает приложение **для этой группы**: большие
   карточки «Покер» и «Дурак», ниже — игры, которые уже открыты в группе, с
   кнопкой «Присоединиться».
3. Выбрали игру → короткие настройки → «Создать лобби». Вы — хост. В группе
   появляется карточка лобби; друзья открывают его по ней или из списка в
   хабе.

В группе может идти несколько игр сразу — у каждой своя карточка, итоги —
отдельным сообщением в конце каждой. `/newgame` по-прежнему сразу делает
покерный стол (один на группу; второй покер — через `/game`).

### Покер

1. В группе `/newgame` (или «Покер» в хабе). Автор — хост. Бот присылает карточку комнаты.
2. Каждый жмёт **«🃏 Открыть стол»** → приложение открывается сразу на этой
   комнате → **«Сесть за стол»**. Код вводить не нужно.
3. Хост в приложении (⋯ → «Настройки стола») выбирает стек, блайнды, таймер
   хода и режим: **карты раздаёт бот** или **настоящие карты и дилер**.
4. Хост жмёт **«▶ Начать»**. Дальше всё в приложении.

За весь вечер в группе появляются **два сообщения**: карточка комнаты (она
правится по ходу игры: «раздача #5 · флоп · 👉 ход: Макс», итог раздачи) и
итоги в конце (`/finish`). Никакого спама после каждого хода.

Команды в группе: `/game` (выбор игры), `/newgame` (сразу покер), `/table`
(карточки игр вниз чата), `/finish` (итоги своей игры, хост), `/cancel`
(удалить свою игру, хост), `/help`. Если вы хост нескольких игр, `/finish` и
`/cancel` берут самую новую; остальные — кнопкой в приложении.

### Стол

- **Свои карты** — всегда внизу, крупно, настоящими картинками (52 SVG).
  Под ними — название комбинации.
- **Чей ход** — подсвеченное место (и кольцо таймера, если он включён) у всех;
  у того, кто ходит, —
  «ВАШ ХОД», подсветка своих карт, другая панель и вибрация.
- **Панель**: FOLD · CHECK или CALL N · RAISE/BET. Когда чек бесплатный, FOLD
  спрятан — самый дорогой случайный тап. RAISE открывает шторку: +BB, ½ POT,
  POT (тап — и ставка сделана), ползунок для своей суммы. **ALL-IN — только со
  второго тапа.**
- **Стадия** — PRE-FLOP / FLOP / TURN / RIVER / SHOWDOWN в шапке.
- **Итог раздачи** — по центру стола; победители подсвечены, у проигравших на
  вскрытии — их карты или «НЕ ПОКАЗАЛ».
- **Стол подстраивается под экран**: место игрока — аватар с картами на нём,
  имя и стек в одной плашке; раскладка считается по настоящему размеру
  стола, и места, карты борда и надписи уменьшаются вместе ровно настолько,
  чтобы ничего ни на что не наезжало — от iPhone SE до окна Telegram
  Desktop. Проверяется `npm run layout`.
- **Выигрыш видно**: фишки летят из банка к победителю, банк тает, а его
  баланс на глазах докручивается до новой суммы — золотом, с «+N» рядом.
- **Движение рассказывает, что произошло**: карты разлетаются по кругу при
  раздаче, борд переворачивается по карте, ставка улетает фишками в банк,
  сброшенные карты уходят в центр, у того, чей ход, — вспышка по краям
  экрана и выезжающие кнопки; последние 10 секунд таймера — красным.
  Открыли стол посреди раздачи — он показывается спокойно, как есть, без
  повторной раздачи. Системная настройка «Уменьшить движение» выключает всё
  это.

### «Ваш ход» в личку

Только если **стол у игрока сейчас закрыт** (приложение свёрнуто) и он хоть
раз нажал Start у бота. В напоминании — раздача, сколько коллировать, банк и
кнопка открыть стол. **Когда ход прошёл, напоминание удаляется** — в личке не
копятся устаревшие «ваш ход». Карт в личке нет никогда.

### Режим дилера (настоящие карты)

Хост включает «Настоящие карты» и назначает **дилера** (⋯ → Хост → Роли).
Дилер сидит за столом, но карт не получает и не играет: он тасует и сдаёт
настоящую колоду, бот ведёт фишки, очередь и банк.

После вскрытия у дилера — **«🏆 Кто выиграл?»**: крупные плашки игроков,
тап — победитель, несколько тапов — делёж. Если банков несколько (сайд-поты),
он проходит их по очереди. Затем экран итога: кто сколько получит — и
**«✅ Подтвердить»**. Только после этого бот раздаёт фишки. Ошибся — хост
может отменить результат до следующей раздачи. Решает дилер; запасной
вариант — хост, чтобы севший телефон дилера не заморозил стол. Игрок сам себе
банк не присудит. (Настоящие карты без назначенного дилера — отмечает любой
сидящий за столом.)

Роль, назначенная посреди раздачи, начинает действовать со следующей.

### Дурак

1. `/game` → «Дурак» → вариант (**подкидной** по умолчанию или
   **переводной**) и таймер хода → «Создать лобби».
2. Друзья: «Присоединиться» → «Сесть за стол». От 2 до 6 игроков. Хост жмёт
   «▶ Начать».
3. **Свои карты** — веером внизу. Что можно сыграть сейчас — ярко,
   остальное приглушено. **Тап — выбрать, второй тап — сыграть.**
   Отбивающийся выбирает карту и тапает по карте на столе, которую кроет.
4. **Кнопки** — только возможные: «ВЗЯТЬ», «ПЕРЕВЕСТИ» (в переводном),
   «БИТО» у атакующего, «ПАС» у подкидывающих.
5. Кто атакует, кто отбивается, кто сказал «пас», кто вышел — плашками у
   соперников; у вас крупно: «ВЫ ХОДИТЕ», «ВЫ ОТБИВАЕТЕСЬ», «МОЖНО
   ПОДКИНУТЬ».
6. Партия кончилась — «🤡 Дурак — Макс» на столе и в карточке группы, счёт
   серии. «Следующая партия» — любой за столом; ходят «под дурака».
7. Хост: ⋯ → «Завершить игру» (или `/finish`) — итоги серии («кто сколько
   раз дурак») одним сообщением в группу.

**Правила** — по pagat.com (Durak, Podkidnoy, Perevodnoy), каждое — тест в
`bot/durak.rules.test.js`; полный список — в
[docs/game-hub.md](../docs/game-hub.md), раздел 5. Коротко: 36 карт, по 6;
козырь — под колодой; первым ходит младший козырь; подкидывают все, кроме
отбивающегося, карты тех достоинств, что на столе, — не больше 6 и не больше,
чем карт у отбивающегося; «бито» — отбившийся ходит, «взял» — пропускает ход;
добор: атаковавший, остальные по кругу, отбивавшийся последним; последний с
картами — дурак, вышли разом — ничья. В переводном отбивающийся может
перевести атаку картой того же достоинства, пока ничего не побито.

**Правила приложения.** «Пас» каждый говорит сам: иначе приложение выдало бы,
у кого нет нужной карты. Ушёл из группы посреди партии или хост удалил —
партия прерывается и не засчитывается. Таймер хода (выключен по умолчанию):
время вышло — отбивающийся берёт, подкидывающие пасуют, начать отбой — бот
ходит младшей некозырной картой.

---

## Два железных правила

### 1. Никто не ходит за другого

Mini App получает от Telegram `initData`, **подписанный токеном бота**
(HMAC-SHA256 по правилам Telegram). Сервер проверяет подпись при открытии
стола, и с этого момента человек в этом соединении — его настоящий Telegram id.
Это вся модель безопасности: ни паролей, ни кодов.

- **Тело сообщения ничего не решает.** `{t:'act', action:'fold'}` — это ход
  того, чей это сокет. id, место, имя в сообщении игнорируются — тест
  подкладывает их все.
- **Подделка, чужой бот, старая сессия** (старше суток) — стол не откроется.
  Проверка сверена с опубликованным примером подписи из документации tma.js,
  а не только с собственным подписывателем.
- **Комната — из подписи.** Код комнаты приходит как `start_param` внутри
  `initData`; страница не может подменить его другим.
- **Открыть стол ≠ сесть.** Смотреть может любой по ссылке, сесть — отдельное
  нажатие. Хост может удалить любого.
- **Отказ всегда озвучен** — всплывающим сообщением («Сейчас ходит Макс»).
- **Анонимные админы** не могут создать стол; **один аккаунт — одно место**,
  даже с двух телефонов.
- **Права хоста** (настройки, роли, докупка, удаление, пауза, итоги) — по id.
- **Единственное исключение — таймер хода**: когда время вышло, бот делает
  бесплатный ход — чек, если можно, иначе фолд. Фишки он не тратит никогда.

### 2. Никто не видит чужих карт

Сервер не рассылает общее состояние: **каждое окно получает своё**, собранное
для этого человека. В нём его две карты; борд — ровно столько, сколько уже
открыто; чужие карты — только тех, кто обязан показать их на вскрытии.
Колоды, будущих карт борда и чужих карманных в данных нет вовсе — их нельзя
выковырять даже из инструментов разработчика.

В группе и в личке карт нет никогда: карточка комнаты говорит только стадию
(«флоп»), а не борд.

**Дурак — так же.** Каждому — его карты; у соперников — только число карт;
колода и сброс — только числами (порядок колоды и сброшенные карты не
покидают сервер); стол и козырь — всем, как за настоящим столом. Карточка в
группе и «ваш ход» в личке называют масть козыря, но ни одной карты.
Кнопки, подсветка карт — только подсказка: сервер проверяет каждый ход
(«эта карта не бьёт 9♠», «подкидывать можно только 7 или 9»).

Тесты проверяют это по **всему**, что каждое окно получило за игру, и по всем
сообщениям в группе, включая правки и удалённые.

---

## Карты

- **Колода** — Фишер–Йетс на `crypto.randomInt`. Сдача — по карте за круг с
  места слева от баттона.
- **Вскрытие** автоматическое; каждый банк — лучшей руке **среди своих
  претендентов** (короткий олл-ин с тузами берёт основной банк, сайд-пот —
  лучшая из остальных). Делёж и лишняя фишка — забота движка.
- **Кто показывает**: победитель спорного банка и все в олл-ине. Если против
  олл-ина остался максимум один игрок с фишками (олл-ин один на один,
  доигрывание борда) — открывают **все** (TDA, «Face Up for All-Ins»).
  Остальные проигравшие сбрасывают молча («НЕ ПОКАЗАЛ» — без карт и без
  названия комбинации): так бывает, когда за сайд-пот ещё торговались двое.
  Возврат неуравненной ставки победой не считается.
- **Олл-ин до ривера** — по TDA: как только торговля закончилась, все руки
  сразу открываются, **потом** борд идёт по улицам — флоп, тёрн по полторы
  секунды. Названий комбинаций до ривера нет: «Флеш до A» выдал бы, что
  придёт. С ривером выигравшая пятёрка поднимается и загорается золотом,
  остальные карты гаснут — и только потом фишки уходят победителю. Стеки и
  итог до ривера не видны — ни на столе, ни в группе, ни в данных телефона.
- **Последняя раздача вечера** доигрывается на столе целиком — и только
  потом приложение показывает итоги (или раньше — кнопкой «🏁 Итоги вечера»).
- **Числа** — с точками: 1.000.000, везде — на столе, в группе, в итогах.

### Правила — официальные

Игра сверена с правилами TDA 2024 (по ним судят крупные турниры) и Robert's
Rules of Poker (что TDA оставляет на усмотрение заведения). Каждое правило —
отдельный тест в `bot/rules.test.js`:

- **Раздача**: по одной карте, с места слева от баттона, два круга; вдвоём
  последняя карта — баттону. **Перед флопом, тёрном и ривером карта
  сжигается** (Robert's Rules, «burn and turn»); её не видит никто.
- **Блайнды и очередь**: малый и большой — слева от баттона; до флопа первым
  ходит игрок слева от большого блайнда, после — первый слева от баттона.
  **Вдвоём** баттон ставит малый блайнд, ходит первым до флопа и последним
  после (TDA, «Button in Heads-up»); **при переходе на игру вдвоём никто не
  ставит большой блайнд два раза подряд**.
- **Ставки (No-Limit)**: минимальная ставка — большой блайнд; рейз — не
  меньше последней полной ставки или рейза; у большого блайнда есть опция.
  **Олл-ин меньше полного рейза не открывает торговлю** тем, кто уже
  действовал, — **если только** несколько таких олл-инов вместе не дают
  полного рейза; поставленный блайнд — не действие (TDA, «Re-Opening the Bet»).
- **Банк**: неуравненная ставка возвращается; каждый банк — лучшей руке
  среди его претендентов; делёж поровну, лишняя фишка — первому победителю
  слева от баттона (TDA, «Awarding Odd Chips»); масти не старшинствуют.
- **Вскрытие**: «карты говорят сами» — комбинации определяет бот; победитель
  без борьбы карты не показывает; олл-ин — лицом вверх до конца борда (TDA).
- **Table stakes** (Robert's Rules): фишки, докупленные во время раздачи, в
  ней не играют — они приходят со следующей.
- **Комбинации**: стрит-флеш › каре › фулл-хаус › флеш › стрит › тройка ›
  две пары › пара › старшая карта; A-2-3-4-5 — младший стрит; через туза
  стрит не переходит (Q-K-A-2-3 — не стрит).
- **Выбор заведения**: баттон **переходящий** (Robert's Rules, «moving
  button») — он всегда идёт к следующему игроку. В турнирах TDA — «мёртвый
  баттон»; для домашней игры переходящий проще и честен так же.

`server/game.js` (движок ставок, общий с веб-версией) не менялся — где он
расходится с правилами, это исправлено в `bot/room.js` и описано в
`ENGINE-NOTE.md`.

### Таймер хода и автораздача

Выключены по умолчанию. Хост включает в настройках: 30 / 60 / 90 / 120 секунд.

- Время вышло — чек или фолд. Два таймаута подряд — игрок «пропускает»
  раздачи, пока сам не вернётся. Новая улица — новое время.
- Следующая раздача — сама через 10 секунд (кнопка — раньше; двух раздач не
  будет). В режиме дилера автораздачи нет: следующую начинает дилер (или
  хост), когда колода перетасована.
- Раздача, в которой никто не сходил сам, ставит стол на паузу — бот не
  раздаёт в пустую комнату.
- Пауза останавливает часы; рестарт бота не съедает время.

### Undo — только для действий хоста

Колоду держит бот, поэтому откат игры был бы эксплойтом (отменил фолд —
посмотрел флоп бесплатно). Любое игровое событие очищает undo. Хост может
отменить свою докупку, смену блайндов, удаление, передачу прав, роль — и,
в режиме дилера, результат раздачи до следующей сдачи.

---

## Ограничения Telegram и что с ними сделано

| Ограничение | Что сделано |
|---|---|
| Кнопка мини-приложения (`web_app`) — только в личке | в группе — URL-кнопка на прямую ссылку `t.me/<бот>/<приложение>?startapp=<код>`; без `MINIAPP` — `t.me/<бот>?start=t_<код>`, и бот отвечает в личке кнопкой `web_app` |
| Mini App — только HTTPS | `WEBAPP_URL` + туннель или хостинг; без адреса бот предупреждает при старте |
| Код комнаты должен дойти до приложения | `startapp` → `start_param` внутри подписанного `initData` |
| Mini App не знает, из какой группы его открыли | карточка `/game` несёт случайный код группы: `startapp=g_<код>`; какой это чат, знает только сервер. Из хаба приложение переходит только в игры этой группы |
| Бот не может написать первым | «ваш ход» в личку — только тем, кто нажал Start; остальным — ничего, и бот даже не пытается |
| ~20 сообщений в минуту в группу | одна карточка на игру, правки склеиваются и не чаще раза в секунду; окна приложения получают состояние мгновенно по WebSocket — там лимитов нет |
| `message is not modified` | текст и кнопки сверяются до отправки |
| Кнопки старой чат-версии в истории | на нажатие отвечают ссылкой на стол — без вечного спиннера |
| Пользователь заблокировал бота | больше не получает напоминаний |
| Группа → супергруппа меняет `chat.id` | стол переезжает, код комнаты и открытые окна продолжают работать |
| Люди выходят из группы | вышедшего складывают, олл-ин доигрывает; если остался один — банк его |
| Бота удалили из группы | стол удаляется, открытые окна получают «игру удалили» |

---

## Как устроено

| Файл | Что делает |
|---|---|
| `index.js` | запуск: `.env`, grammY (long polling), HTTP+WS-сервер |
| `app.js` | ядро: команды группы, `/game`, игры группы, карточки, напоминания, таймеры |
| `hub.js` | ядро: WebSocket-сессии, `initData`, хаб группы, переходы в лобби |
| `games/index.js` | список игр — ядро знает игры только через него |
| `games/poker/` | модуль покера: обёртка над `room.js`, `view.js`, `render.js`, `cards.js` |
| `games/durak/` | модуль дурака: `rules.js` (правила), `view.js` (что видит один), `render.js` (тексты) |
| `server.js` | раздаёт `miniapp/`, `/ws`, `/health`; лимиты сообщений |
| `webapp-auth.js` | проверка подписи `initData` |
| `view.js` | состояние стола глазами одного человека (приватность здесь) |
| `room.js` | комната: игроки, роли, раздачи, победители, таймеры, undo |
| `cards.js`, `deck.js`, `eval.js` | колода, сдача, вскрытие, оценка рук |
| `render.js` | тексты для Telegram: карточка, «ваш ход», итоги |
| `store.js` | SQLite (`node:sqlite`): игры (по коду), группы, кто нажал Start |
| `../miniapp/` | само приложение: `app.js` (оболочка), `ui.js`, `net.js`, `hub.js`, `games/poker.js`, `games/durak.js`, стили, `cards/*.svg` |

Приложение — без сборки и фреймворков: один JS-файл, работает в WebView
Telegram на iOS и Android. После обрыва связи переподключается само и
получает состояние целиком.

### Состояние и перезапуски

Комнаты в памяти, копия в SQLite при каждом изменении. После рестарта — та же
раздача, те же карты, тот же игрок на часах; открытые окна переподключаются.

### Честно о доверии

**Кто запускает бота, тот технически может прочитать базу** — там колода
текущей раздачи. Для компании друзей это обычное «кто-то хостит»; бот эти
данные нигде не показывает и в логи не пишет. В режиме дилера колоды в базе
нет вовсе.

### Хостинг

Процесс должен не засыпать и быть доступен по HTTPS. Ноутбук + cloudflared —
для вечера с друзьями; для постоянной игры — VPS или Fly.io с доменом
(`PORT` за HTTPS-прокси). Бесплатный Render засыпает через 15 минут тишины —
не подходит.

---

## Проверка

```bash
npm run go        # туннель + бот одной командой (см. «Запуск одной командой»)
npm test          # всё: 60 тестов веб-версии + 271 тест бота и инструментов
npm run test:bot  # только бот
npm run preview   # скриншоты: стол покера, хаб, лобби и стол дурака — в preview/ (нужен Playwright)
npm run e2e       # браузеры играют тапами: две раздачи покера и целую партию дурака (нужен Playwright)
npm run layout    # покер 2–8 и дурак 2–6 игроков × 12 размеров экрана: ничего ни на что не наезжает
npm run cards     # пересобрать 52 SVG-карты
```

Telegram заменён стабом (`tg-stub.js`), который не поддакивает: отказывает в
личке тем, кто не нажал Start (403), запрещает кнопку `web_app` в группе,
воспроизводит «message is not modified». Приложение в тестах — настоящий хаб
с настоящей проверкой подписи; время — фальшивые часы.

- **`webapp-auth.test.js`** — подпись: чужой пример из документации, подмена
  любого поля, чужой бот, старая сессия, мусор.
- **`identity.test.js`** — никто не ходит за другого: чужая подпись, чужая
  комната, не свой ход, id и место в теле сообщения, зритель, права хоста,
  дилер решает банк, одно место на аккаунт, каждый отказ озвучен.
- **`cards.test.js`** — чужие карты: всё, что получило каждое окно, и вся
  группа; кто показывает; борд по улицам; лучшая рука; делёж; сайд-поты;
  длинная сессия с докупками.
- **`dealer.test.js`** — режим дилера: роли, дилер без карт, выбор
  победителя, делёж, сайд-поты по шагам, подтверждение, отмена.
- **`timer.test.js`** — таймер хода, пропуски, автораздача, пауза, рестарт,
  олл-ин-борд по улицам без спойлеров.
- **`app.test.js`** — бот как лобби: ссылка ведёт в нужную комнату, за вечер
  ровно два сообщения в группе, «ваш ход» только закрытому столу и удаляется
  после хода; серия ходов — одна правка карточки; удалённая карточка
  заменяется; скрытый ALL-IN и сумма вне диапазона отклоняются сервером;
  undo хоста, но не ставки; двойное нажатие; сохранение фишек; уход из
  группы; миграция; рестарт.
- **`render.test.js`** — снапшоты карточки, напоминания, итогов; размеры
  ставок в шторке.
- **`hub.test.js`** — хаб: `/game`, ссылка на хаб группы (случайный код, не
  id чата), создание лобби, переход только в игры своей группы, двойной тап,
  несколько игр в группе, `/table`, `/finish`, `/cancel` только своей игры,
  старые ссылки, перенос старой базы, рестарт.
- **`durak.rules.test.js`** — правила дурака по одному (1–16 и правила
  приложения): колода, раздача, козырь, первый ход, «под дурака», подкидывание,
  предел отбоя, отбой, бито, взял, добор, выход, дурак и ничья, перевод, серия,
  пас, таймер, двойной тап.
- **`durak.test.js`** — дурак через приложение: всё, что получил каждый
  телефон за партию, — только свои карты, стол и козырь; в группе и в личке
  карт нет; никто не ходит за другого (id, место, имя в сообщении
  игнорируются); отказы словами; за серию — два сообщения в группе; «ваш ход»
  защитнику и подкидывающим — только закрытым, удаляется; таймер; рестарт.
- **`rules.test.js`** — официальные правила по одному: раздача и сжигание,
  блайнды и очередь, игра вдвоём, минимальный рейз, опция блайнда, короткие
  олл-ины, открытие карт, лишняя фишка, старшинство комбинаций, table stakes.
- **`eval.test.js`**, **`store.test.js`** — оценщик рук и база.

Главные проверки подтверждены мутациями: в код вносились утечка чужих карт в
окно, личность из тела сообщения, непроверяемая подпись, вечная сессия, комната
из страницы вместо подписи, напоминание открытому столу, неудаляемое
напоминание, выигрыш, видимый до конца открытия борда, весь борд сразу,
снятая защита от скрытого олл-ина, правка карточки на каждый ход — каждый раз
краснели нужные тесты.

---

## Чего здесь нет

- **Выбора «показать / сбросить» на вскрытии** — правило фиксированное (см.
  «Кто показывает»): иначе вскрытие ждало бы каждого проигравшего.
- **Игры командами в чате.** Раньше ставки делались `/call`, `/raise 300`;
  теперь всё в приложении — одним тапом, без опечаток и без спама в группе.
- **Карт в личке.** Свои карты — на столе, приватно для каждого.
